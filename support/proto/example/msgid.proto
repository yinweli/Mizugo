syntax = "proto3";

option go_package = "/messages;messages";

// 訊息編號
enum MsgID {
  Unknown = 0; // 不明/錯誤訊息編號, 此編號不可使用
  KeyReq = 1; // 要求密鑰
  KeyRes = 2; // 回應密鑰
  PingReq = 3; // 要求Ping
  PingRes = 4; // 回應Ping
}
