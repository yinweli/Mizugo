syntax = "proto3";

option go_package = "/msgs;msgs";

// 訊息編號
enum MsgID {
  Unknown = 0; // 不明/錯誤訊息編號, 此編號不可使用
  JsonQ = 1; // 要求Json
  JsonA = 2; // 回應Json
  ProtoQ = 3; // 要求Proto
  ProtoA = 4; // 回應Proto
  PListQ = 5; // 要求PList
  PListA = 6; // 回應PList
}
