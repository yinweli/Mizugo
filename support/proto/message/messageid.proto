syntax = "proto3";

option go_package = "/messages;messages";

// 訊息編號
enum MsgID {
  Unknown = 0; // 不明/錯誤訊息編號, 此編號不可使用
  Echo = 1; // 回音訊息
  ReqKey = 2; // 要求密鑰
  ResKey = 3; // 回應密鑰
  ReqPing = 4; // 要求Ping
  ResPing = 5; // 回應Ping
}
