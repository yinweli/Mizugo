## Startup Instructions

- At the start of every new conversation, if `CLAUDE.md` exists in the repository root, read it before taking any other action.
- Treat `CLAUDE.md` as repository-specific working instructions unless it conflicts with higher-priority system or developer instructions.
- When working in a subdirectory, also check for the nearest local `CLAUDE.md` and follow it if present.
